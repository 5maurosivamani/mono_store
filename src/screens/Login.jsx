import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { FontAwesome } from '../global/Icons'
import ScreenLayout from '../layout/ScreenLayout'

const Login = () => {
  return (
    <ScreenLayout>
        <Text>Login</Text>
    </ScreenLayout>
  )
}

export default Login

const styles = StyleSheet.create({})